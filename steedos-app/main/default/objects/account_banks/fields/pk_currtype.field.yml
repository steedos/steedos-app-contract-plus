name: pk_currtype
defaultValue: |-
  function anonymous(
  ) {

    if(Meteor.isClient) {
      return '1002Z0100000000001K1';
    }

  }
label: 币种
reference_to: currency
required: true
sort_no: 210
type: lookup
