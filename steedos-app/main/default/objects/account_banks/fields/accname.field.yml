name: accname
label: 户名
type: text
required: true
defaultValue: |-
  function anonymous(
  ) {

    if(Meteor.isClient){
      var account = Creator.getObjectRecord();
      if (account) {
        return account.name;
      }
      return '';
    }

  }
sort_no: 120
